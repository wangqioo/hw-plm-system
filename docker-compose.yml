services:
  plm-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: plm-system
    restart: unless-stopped
    ports:
      - "8080:80"
    labels:
      - "description=硬件研发物料管理系统 PLM"
